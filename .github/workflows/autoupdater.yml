name: autoupdater
run-name: autoupdater
on: [push]
jobs:
  python_script:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
        with:
          token: ${{ secrets.PAT }}

      - uses: actions/setup-python@v4
        with:
          python-version: '3.8'

      - run: pip install -r requirements.txt
      - run: cd _work
      - run: python updater.py
      - run: cd
      - run: pwd

      - name: Push generated files to target repository
        uses: cpina/github-action-push-to-another-repository@main
        env:
          API_TOKEN_GITHUB: ${{ secrets.API_TOKEN_GITHUB }}
        with:
          source-directory: '_work'
          destination-github-username: 'rnckp'
          destination-repository-name: 'starter-code-opendataswiss'
          target-branch: main



      # - uses: GuillaumeFalourd/copy-push-files@v1
      #   with:
      #     commit_message: auto update all files
      #     target_branch: main
      #     source_files: index.md README.md 01_r-markdown 02_python
      #     remote_repository: https://github.com/rnckp/starter-code-opendataswiss
      #     access_token: ${{ secrets.PAT }}


      # - uses: stefanzweifel/git-auto-commit-action@v4
      #   with:
      #     commit_message: Update all files 